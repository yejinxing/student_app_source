# 🎉 学生信息处理工具 v1.0.1

## 📋 更新内容

### 📄 Excel 转 Word 功能优化

#### 统计表格样式改进
- **列宽规范化**：
  - 总统计表系部列宽设置为 2.19cm
  - 分层统计表（子表格）系部列宽设置为 3.61cm
  - 班级数、总计列宽统一为 1.32cm
  - 子列（层次、性别、政治面貌、民族）宽度统一为 1.2cm

- **表头行高设置**：
  - 第一行表头行高固定为 0.8cm
  - 第二行表头行高固定为 1.2cm

- **表格布局优化**：
  - 使用固定列宽布局，确保样式精确生效
  - 表格整体居中对齐

- **内容显示优化**：
  - 子列表头内容移除空格和换行符，显示更紧凑
  - 统计表格中空值统一显示为数字 0，不再留空

#### 页面设置调整
- **页面边距统一**：
  - 上边距：0.98cm
  - 下边距：1.04cm
  - 左边距：2.58cm
  - 右边距：1.98cm
- **页眉页脚边距**：
  - 页眉距顶部：0.9cm
  - 页脚距底部：0.6cm

#### 段落格式调整
- 各层次统计表标题（如"2024年本科新生情况统计表"）新增段前 60 磅间距

#### 层次判断逻辑优化
- 层次（本科/专科/专升本）判断采用双重机制：
  1. **优先从表格"层次"字段获取**（如有该字段）
  2. **备选根据班级名称推断**（如表格无层次字段）：
     - `gb` 或 `本科` → 本科
     - `gz` 或 `专科` → 专科
     - `gbs` 或 `专升本` → 专升本
- 学制字段直接使用表格中的原始内容，不做推理转换
- 层次与学制完全独立，互不影响

## 🔧 技术改进
- 使用 `TableLayoutType.FIXED` 和 `columnWidths` 实现精确的表格列宽控制
- 优化数据单元格的空值处理逻辑
- 层次与学制分离，逻辑更清晰准确

---

**开发者**：yejinxing  
**邮箱**：yejinxing1728@qq.com  
**开源协议**：GPL-3.0 License
